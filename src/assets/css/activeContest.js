import { makeStyles } from "@mui/styles";

export const activeContest = makeStyles((theme) => ({
  activeContestBox: {
    background: "#A60057",
    color: "#fff",
    padding: "16px 18px",
    borderRadius: "13px",
    textAlign: "center",
  },
}));
